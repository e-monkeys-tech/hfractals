module Main where

import HaskellSay (haskellSay)

import LineGraphics
-- import Graphics.HGL
import Fractals

main :: IO () 
main = 
      haskellSay "Hello ! This project don't yet allow draw fractals but fix comes soon"
      -- fractalTree 0.35 23 ((400, 400) (440, 440))